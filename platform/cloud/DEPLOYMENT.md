# Kalix Code — النشر والإدارة

## نشر رابط HTTPS ثابت

نسخة Kalix Code الجاهزة للنشر محفوظة عند الإصدار `bd19ff70`. بعد اكتمال النشر، تعرض الواجهة رابط HTTPS ثابتًا بصيغة `https://<معرّف-الموقع>.manus.space`؛ انسخه واستخدمه كرابط Kalix Code العام. لا تستخدم رابط المعاينة المؤقت للاختبار الخارجي أو المشاركة الدائمة.

## حالة النشر الحالية

نطاق Kalix Code الثابت هو `https://kalixcode-lvx9ubmm.manus.space`. اكتمل نشر إصدار التوسعة `bd19ff70` بنجاح؛ يتضمن هذا الإصدار إدارة مستودعات GitHub وفلترة النماذج والدليل التشغيلي `KALIX_CODE_GUIDE.md`.

في التحقق الأخير، حمّل النطاق الثابت الحزمة `assets/index-D-aXdub9.js` لمسار `/workspace/github` وطبّق حارس تسجيل الدخول على المساحة المحمية. كما احتوت الحزمة المنشورة على تسمية **مستودعات GitHub** ومسار `/workspace/github` وإجراء `github.repositories`. هذا يؤكد أن صفحة الإنتاج تخدم إصدار التوسعة، مع بقاء تفاصيل الحساب والمستودعات غير ظاهرة قبل المصادقة.

## العنوان المخصص

إذا رغبت في عنوان مخصص، افتح **Settings → Domains** بعد النشر، ثم أضف نطاقك أو اشترِ نطاقًا واربطه. لا يُعد النطاق المخصص نشطًا حتى تكتمل عملية الربط والتحقق داخل لوحة الإدارة. يبقى رابط `manus.space` الثابت متاحًا كعنوان تشغيل افتراضي.

## سلوك الوصول والإدارة

صفحة `/` عامة، بينما تتطلب `/workspace` تسجيل الدخول عبر Manus OAuth. تظهر مزوّدات النماذج ونماذجها للحساب المالك فقط؛ لا تعيد واجهة التطبيق مفتاح API المحفوظ أو النص المشفر. لا تضع مفاتيح المزودات في عنوان الرابط أو وصف المشروع أو لقطات الشاشة.

## حدود التشغيل

تدعم ميزة الاكتشاف حاليًا واجهات مزودات متوافقة مع OpenAI من خلال المسار `/models`. عند فشل الاكتشاف، يمكن إضافة النماذج يدويًا من مساحة العمل. عنوان المزوّد يجب أن يستخدم HTTPS ولا يسمح بالعناوين المحلية أو الخاصة أو المحجوزة. يبقى على طبقة الاستضافة إضافة سياسات خروج شبكي مناسبة إذا تطلبت بيئة الإنتاج دفاعًا إضافيًا ضد DNS rebinding المتقدم.
